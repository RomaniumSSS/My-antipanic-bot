# Баги и UX-проблемы (17.12.2025)

Выявлены в реальном user testing (ID: 579067869).

---

## 🚨 Критичные баги (блокируют работу)

### 1. ~~`/evening` (Вечер) падает с ошибкой~~ ✅ FIXED (17.12.2025)
**Где**: при клике на кнопку "Вечер" или команде `/evening`
**Что**: `❌ Произошла ошибка. Попробуй ещё раз или напиши /start`
**Воспроизведение**: постоянно, независимо от состояния
**Исправление**: добавлено debug логирование в `evening.py` и `complete_daily_reflection.py` для выявления причины падения

### 2. `/morning` (Утро) падает на первом касании
**Где**: первый вызов `/morning` после регистрации
**Что**: `❌ Произошла ошибка. Попробуй ещё раз или напиши /start`
**Воспроизведение**: только первый раз, потом работает

### 3. `/stuck` (Застрял) не видит активную цель после переключения
**Где**: смена цели через "Цели" → "Возобновить" → `/stuck`
**Что**: `Не получилось определить контекст: Нет активного этапа для цели`
**Причина**: FSM state не обновляется при смене активной цели через кнопки

### 4. ~~Огромный список всех шагов за все время~~ ✅ FIXED (17.12.2025)
**Где**: после выполнения любого шага показывается "Шаги на сегодня"
**Что**: список содержит ВСЕ шаги за все дни (15+ пунктов), а не только сегодняшние
**Воспроизведение**: постоянно
**Исправление**: добавлена фильтрация по `scheduled_date=today` в `session.py:log_antipanic_action()` перед добавлением новых шагов

---

## 😕 UX-проблемы (не ломают, но критично портят опыт)

### 5. Нет онбординга при /start
**Проблема**: пользователь не понимает, что делать
- Нет приветствия/объяснения, что это за бот
- Нет инструкции "что нажимать"
- Нет последовательности действий (квиз → онбординг → утро)
**Цитата**: _"Вообще, ты заходишь в бота, непонятно, как им пользоваться, для чего он нужен, что это такое, что нажимать базово"_

### 6. Нет кнопки для апки
**Проблема**: нужно знать команду `/app`, чтобы открыть мини-апп
**Решение**: добавить кнопку "📱 Открыть приложение" в главное меню

### 7. Апка бесполезна (нет УТП)
**Проблема**: мини-апп дублирует функции бота, непонятно, зачем она нужна
- Показывает те же данные, что и бот
- Нет уникальных фич
- Нет объяснения преимуществ
**Цитата**: _"Что пользователи получали за счёт использования апки вместо бота? По сути дела, ничего"_

**Возможные решения**:
- Убрать часть функций из бота, оставить только в апке (статистика, графики)
- Добавить уникальные фичи в апку (календарь, визуализация прогресса)
- Добавить уведомления/призыв использовать апку

### 8. Кнопка "Не знаю с чего" — текст не влезает
**Проблема**: текст "🤷 Не знаю с чего" обрезается, непонятная формулировка
**Решение**: переименовать в "🤷 Не знаю" или "🆘 Застрял"

### 9. Разморозка однотипная
**Проблема**: AI генерирует одно и то же действие: _"Пройдися по комнате или коридору 2 минуты, считая шаги до 120"_
**Решение**: добавить вариативность в промпт, несколько шаблонов

### 10. Орфографическая ошибка
**Где**: разморозка, микрошаги
**Что**: "Пройдися" → правильно: "Пройдись"

### 11. ~~Кнопка "Главное меню" дублируется в stuck flow~~ ✅ FIXED (17.12.2025)
**Проблема**: в `stuck.py` после выбора действия "Делаю" показывалась дважды подряд
**Исправление**: убран дублирующий вызов `main_menu_keyboard()` в `stuck.py:microhit_feedback()`

---

## Приоритеты исправления

### ✅ P0 (критично, блокирует) — ИСПРАВЛЕНО (17.12.2025):
1. ~~Фикс `/evening` (падает всегда)~~ — добавлено логирование
2. ~~Фикс "Шаги на сегодня" (показывает все шаги)~~ — фильтрация по дате
3. ~~Кнопка "Главное меню" дублируется~~ — убран дубль

### P0 (критично, требует внимания):
4. Фикс `/stuck` после смены цели
5. Фикс `/morning` (первый запуск падает)

### P1 (важно, портит UX):
6. Онбординг при `/start`
7. Кнопка для апки
8. Исправить "Пройдися" → "Пройдись"

### P2 (желательно):
9. Вариативность разморозки
10. УТП для апки (или убрать?)
11. Переименовать кнопки

---

## Тестовый сценарий для проверки

1. `/start` → должен быть онбординг
2. Пройти квиз → создать цель
3. `/morning` → не должно падать
4. Выполнить шаг → "Шаги на сегодня" = только сегодняшние
5. Сменить цель через "Цели" → `/stuck` → должен работать
6. `/evening` → не должно падать
7. Проверить орфографию "Пройдись"
8. Кнопка "📱 Приложение" видна в меню

