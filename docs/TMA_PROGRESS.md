# TMA Migration Progress

**Ветка**: `feature/tma-migration`
**Дата старта**: 2025-12-12

---

## Общий прогресс

- [x] Этап 1.1: Удаление устаревших фич ✅
- [x] Этап 1.2: Упростить onboarding ✅
- [x] Этап 1.3: Упростить morning flow ✅
- [x] Этап 1.4: Упростить evening flow ✅
- [x] Этап 1.5: Чистка states.py ✅
- [ ] Этап 2: Разделить слои (по сценариям, 3-5 дней) 🔄
  - [x] 2.1: Выполнение/пропуск шага (Complete/Skip Step Use-Case) ✅
  - [x] 2.2: Утренний сценарий (Morning Assignment Use-Case) ✅
  - [x] 2.3: Stuck-логика + варианты (Stuck Resolution Use-Case) ✅
  - [x] 2.4: Вечерний сценарий (Evening Reflection Use-Case) ✅
  - [x] 2.5: Закрыть баг с "Изменить" ✅
  - [ ] 2.6: Финальная проверка
- [ ] Этап 3: Деплой бота + БД (1-2 дня)
- [ ] Этап 4: TMA MVP (3-5 дней)
- [ ] Этап 5: Проактивность (1 день)

---

## ✅ Этап 1.1: Удаление устаревших фич (ЗАВЕРШЕН)

**Дата**: 2025-12-12

### Что сделано:

1. ✅ Создан `docs/BACKLOG.md` с описанием:
   - Отложенных фич (quiz, weekly, health)
   - Улучшений (Claude Sonnet 4.5, жесткий тон, геймификация, калибровка)
   - Критичных багов (кнопка "Изменить" удаляет цель)

2. ✅ Удалены файлы:
   - `src/bot/handlers/health.py`
   - `src/bot/handlers/weekly.py`
   - `src/bot/handlers/quiz.py`

3. ✅ Обновлен `src/bot/handlers/__init__.py`:
   - Убраны импорты `health_router`, `quiz_router`, `weekly_router`
   - Убраны роутеры из `dp.include_routers()`

4. ✅ Удалена модель `QuizResult` из `src/database/models.py`

### Результат:

**Было**:
- `quiz.py` (10KB) + `weekly.py` (5.2KB) + `health.py` (359B) = ~15.5KB кода
- QuizResult модель в БД

**Стало**:
- Только ядро: start, onboarding, morning, steps, stuck, evening
- Нет QuizResult модели

**Следующий шаг**: Этап 1.2 - Упростить onboarding (убрать AI генерацию этапов)

---

## ✅ Этап 1.2: Упростить onboarding (ЗАВЕРШЕН)

**Дата**: 2025-12-12

**Цель**: Убрать AI генерацию этапов, оставить только создание Goal + 1 дефолтный Stage "Начало"

### Что сделано:

1. ✅ Упрощен `src/bot/handlers/onboarding.py`:
   - Убран импорт `ai_service` и `ConfirmCallback`
   - Удален вызов `ai_service.decompose_goal()`
   - Убрана логика подтверждения этапов (состояние `confirming_stages`)
   - Теперь создается Goal + 1 Stage "Начало" сразу после ввода дедлайна
   - Добавлены AICODE-NOTE комментарии

2. ✅ Обновлен `src/bot/states.py`:
   - Удалено состояние `OnboardingStates.confirming_stages`
   - Добавлен AICODE-NOTE комментарий

3. ✅ Упрощен `src/bot/handlers/start.py`:
   - Убран импорт `quiz.py` и `QuizResult` модели
   - Убрана логика проверки квиз-истории
   - Новые пользователи сразу переходят к созданию цели (без квиза)
   - Добавлены AICODE-NOTE комментарии

4. ✅ Проверка импортов: успешно

### Результат:

**Было**:
- `onboarding.py`: 236 строк с AI генерацией этапов и подтверждением
- `start.py`: логика с квизом и проверкой QuizResult
- Состояние `OnboardingStates.confirming_stages`

**Стало**:
- `onboarding.py`: 165 строк, прямое создание Goal + Stage "Начало"
- `start.py`: упрощен, прямой переход в onboarding
- Убрано состояние `confirming_stages`
- Все файлы с AICODE комментариями для будущей поддержки

**Следующий шаг**: Этап 1.3 - Упростить morning flow

---

## ✅ Этап 1.3: Упростить morning flow (ЗАВЕРШЕН)

**Дата**: 2025-12-12

**Цель**: Оставить только AntipanicSession, убрать legacy MorningStates

### Что сделано:

1. ✅ Проанализирован `src/bot/handlers/morning.py`:
   - Уже использует только AntipanicSession
   - Нет legacy MorningStates кода
   - Изменений не требуется

2. ✅ Упрощен `src/bot/states.py`:
   - Удалён неиспользуемый `QuizStates` (квиз удалён в Этапе 1.1)
   - Удалён неиспользуемый `GoalStates` (дубликат OnboardingStates)
   - Оставлен `OnboardingSprintStates` (используется в steps.py для пейволла)
   - Добавлен AICODE-NOTE комментарий

3. ✅ Проверка импортов: успешно

### Результат:

**Было** (states.py):
- 7 StatesGroup классов (73 строки)
- QuizStates (неиспользуемый)
- GoalStates (неиспользуемый)

**Стало** (states.py):
- 5 StatesGroup классов (61 строка)
- Только используемые states
- AICODE комментарии для будущей поддержки

**morning.py**:
- Не изменялся (уже упрощён)
- 283 строки с чистым AntipanicSession флоу

**Следующий шаг**: Этап 1.4 - Упростить evening flow

---

## ✅ Этап 1.4: Упростить evening flow (ЗАВЕРШЕН)

**Дата**: 2025-12-12

**Цель**: Короткий итог дня без длинной рефлексии

### Что сделано:

1. ✅ Упрощен `src/bot/handlers/evening.py`:
   - Убран импорт `RatingCallback` и `rating_keyboard`
   - Удалена функция `show_rating_prompt()` → заменена на `finish_day()`
   - Удален callback handler `process_rating()` для обработки оценок 1-5
   - Убраны мотивационные сообщения в зависимости от оценки
   - Убрано сохранение `day_rating` в `DailyLog`
   - Оставлен показ шагов с отметками ✅/⏭/⬜
   - Оставлены обновление streak и XP
   - Добавлены AICODE-NOTE комментарии

2. ✅ Обновлен `src/bot/states.py`:
   - Удалено состояние `EveningStates.rating_day`
   - Добавлен AICODE-NOTE комментарий

3. ✅ Проверка импортов: успешно

### Результат:

**Было** (evening.py):
- 219 строк с оценкой дня (1-5) и мотивационными сообщениями
- Состояние `EveningStates.rating_day`
- Сохранение `day_rating` в DailyLog

**Стало** (evening.py):
- 190 строк (~13% сокращение)
- Прямое завершение дня без оценки
- Фокус на главном: шаги → XP → streak
- AICODE комментарии для будущей поддержки

**states.py**:
- Убрано состояние `rating_day` из `EveningStates`
- Добавлен AICODE-NOTE с объяснением

### ⚠️ Trade-offs:

**Потери:**
- Нет данных о субъективной оценке дня (поле `day_rating` остаётся пустым)
- Нет аналитики настроения пользователя
- Менее эмоциональное взаимодействие (без мотивационных сообщений)

**Выигрыш:**
- Быстрее завершение дня (меньше кликов)
- Упрощённый код (меньше состояний FSM)
- Фокус на объективных метриках (выполненные шаги, streak)

**Возможная доработка после TMA MVP:**
- Автоматическая мотивация на основе streak (без запроса оценки)
- Автоматическая оценка на основе % выполненных шагов

**Следующий шаг**: Этап 2 - Разделить слои (handlers → services → domain)

---

## ✅ Этап 1.5: Чистка states.py (ЗАВЕРШЕН)

**Дата**: 2025-12-12 (объединено с Этапами 1.3 и 1.4)

**Цель**: Удалить все неиспользуемые состояния

### Что сделано:

**Этап 1.3:**
1. ✅ Удалён `QuizStates` (неиспользуемый, квиз удалён в Этапе 1.1)
2. ✅ Удалён `GoalStates` (неиспользуемый, дубликат OnboardingStates)
3. ✅ Оставлен `OnboardingSprintStates` (используется в steps.py для пейволла)

**Этап 1.4:**
4. ✅ Удалено состояние `EveningStates.rating_day` (оценка дня убрана)

### Результат:

**Было** (после Этапа 1.2): 7 StatesGroup классов, 73 строки
**Стало** (после Этапа 1.4): 5 StatesGroup классов, 59 строк

**Оставшиеся states** (минимальный набор для MVP):
- ✅ `OnboardingStates` (2 состояния) - создание первой цели
- ✅ `AntipanicSession` (5 состояний) - утренний флоу
- ✅ `StuckStates` (4 состояния) - помощь при ступоре
- ✅ `EveningStates` (2 состояния) - вечерний отчёт (упрощённый)
- ✅ `OnboardingSprintStates` (1 состояние) - мини-спринт (используется в steps.py)

**Примечание**: Этап завершён полностью, все неиспользуемые состояния удалены.

---

## 🔄 Этап 2: Разделить слои (В РАБОТЕ)

**Дата старта**: 2025-12-12

**Цель**: Подготовить код для TMA, разделив бизнес-логику от интерфейса. Создать структуру, где Этап 3/4 становятся механическими.

**Подход**: НЕ "переписать всё", а по одному сценарию использования за раз. После каждого sub-stage: тестирование руками + коммит.

### Архитектурные слои (итоговая структура):

```
src/
├── core/              # Бизнес-логика (без aiogram/fastapi)
│   ├── domain/        # Чистые правила (без БД, без Telegram)
│   │   ├── gamification.py  # calculate_xp, update_streak ✅
│   │   ├── step_rules.py    # can_complete_step, can_skip_step ✅
│   │   └── stuck_rules.py   # is_stuck, generate_microhit_options
│   └── use_cases/     # Сценарии использования
│       ├── complete_step.py  # ✅
│       ├── skip_step.py      # ✅
│       ├── assign_morning_steps.py
│       ├── resolve_stuck.py
│       └── complete_daily_reflection.py
├── storage/           # Репозитории (тупые CRUD)
│   ├── user_repo.py    # ✅
│   ├── goal_repo.py
│   ├── step_repo.py    # ✅
│   ├── stage_repo.py   # ✅
│   └── daily_log_repo.py  # ✅
└── bot/               # aiogram handlers (thin layer)
    ├── handlers/
    │   └── steps.py    # ✅ (thin handler)
    ├── keyboards.py
    └── states.py
```

### Sub-stages:

---

#### ✅ 2.1: Выполнение/пропуск шага (Complete/Skip Step Use-Case)

**Статус**: Завершен
**Дата**: 2025-12-12
**Коммит**: `refactor(stage-2.1): extract complete/skip step to use-cases`

**Что сделано**:
1. ✅ Создать репозитории:
   - `storage/step_repo.py` — get_step, mark_completed, mark_skipped
   - `storage/user_repo.py` — get_user, update_xp, update_streak
   - `storage/daily_log_repo.py` — get_or_create_daily_log, log_completion, log_step_skip
   - `storage/stage_repo.py` — get_stage, update_stage_progress, mark_stage_completed

2. ✅ Создать домейн-функции:
   - `core/domain/gamification.py` — calculate_xp_reward(step), calculate_streak(user, date)
   - `core/domain/step_rules.py` — can_complete_step(step), can_skip_step(step)

3. ✅ Создать use-cases:
   - `core/use_cases/complete_step.py` — CompleteStepUseCase
   - `core/use_cases/skip_step.py` — SkipStepUseCase

4. ✅ Обновить handler:
   - `src/bot/handlers/steps.py` — использовать use-cases (thin handler)
   - Удалена функция `update_stage_progress` (теперь внутри use-case)
   - Handler теперь только вызывает use-case и показывает результат

5. ✅ Проверить импорты: успешно

6. ✅ Коммит: `refactor(stage-2.1): extract complete/skip step to use-cases`

### Результат:

**Было** (steps.py):
- Вся бизнес-логика в handler (XP, streak, DailyLog, stage progress)
- Функция `update_stage_progress` в handler (54 строки)
- 485 строк кода

**Стало**:
- **Handler** (steps.py): тонкий, только UI-логика (клавиатуры, сообщения, FSM)
- **Репозитории** (storage/): тупые CRUD, без бизнес-логики
- **Домейн** (core/domain/): чистые функции без БД
- **Use-cases** (core/use_cases/): оркестрация репозиториев + домейн

**Следующий шаг**: Этап 2.2 - Утренний сценарий (Morning Assignment Use-Case)

---

#### ✅ 2.2: Утренний сценарий (Morning Assignment Use-Case)

**Статус**: Завершен
**Дата**: 2025-12-13
**Коммит**: `5bf5667` - refactor(stage-2.2): extract morning assignment to use-case

**Что сделано**:
1. ✅ Создан `storage/goal_repo.py`:
   - get_active_goal, get_active_goals
   - get_active_stage, get_all_active_stages
   - get_pending_stages, get_all_stages
   - create_stage, update_stage_status, save_goal, save_stage

2. ✅ Создан `core/domain/step_generation.py`:
   - energy_from_tension(tension) — конвертация напряжения в энергию
   - select_step_difficulty(energy) — выбор сложности шага
   - calculate_max_step_duration(energy, is_micro) — расчет длительности
   - calculate_xp_for_step(difficulty, duration) — расчет XP
   - should_offer_deepen(before, after) — предложение углубления
   - calculate_steps_count_by_energy(energy) — количество шагов

3. ✅ Создан `core/use_cases/assign_morning_steps.py`:
   - AssignMorningStepsUseCase с методами:
     - ensure_active_stage(goal) — обеспечение активного этапа
     - get_body_micro_action(user) — получение телесного действия
     - create_body_step(user, goal, tension) — создание body шага
     - create_task_micro_step(user, goal, tension, max_minutes) — создание task шага

4. ✅ Обновлен `src/storage/daily_log_repo.py`:
   - Добавлена функция log_step_assignment(daily_log, step_id, energy, mood)

5. ✅ Обновлен `src/storage/step_repo.py`:
   - Добавлена функция create_step(stage_id, title, difficulty, ...)

6. ✅ Рефакторирован `src/bot/handlers/morning.py`:
   - Использует AssignMorningStepsUseCase вместо session_service
   - Handler стал тонким — только UI логика
   - Бизнес-логика вынесена в use-case

7. ✅ Обновлен `src/storage/__init__.py`:
   - Экспорт goal_repo для импорта через `from src.storage import goal_repo`

8. ✅ Проверены импорты и синтаксис: успешно
9. ✅ flake8 чист (игнорируя E501 длинные строки)

### Результат:

**Было** (session.py + morning.py):
- Вся логика создания шагов в services/session.py
- Handler напрямую вызывает session_service
- 268 строк в session.py (ensure_active_stage, create_body_step, get_task_micro_action)

**Стало**:
- **Handler** (morning.py): тонкий, только UI-логика (callback handling, FSM, keyboards)
- **Репозитории** (goal_repo, step_repo, daily_log_repo): тупые CRUD без бизнес-логики
- **Домейн** (step_generation.py): чистые функции расчета (energy, difficulty, XP)
- **Use-case** (assign_morning_steps.py): оркестрация репозиториев + AI + domain rules

**Следующий шаг**: Этап 2.3 - Stuck-логика + варианты (Stuck Resolution Use-Case)

---

#### ✅ 2.3: Stuck-логика + варианты (Stuck Resolution Use-Case)

**Статус**: Завершен
**Дата**: 2025-12-13
**Коммит**: `6adfe69` - feat(stage-2.3): add stuck resolution use-case with multiple microhit options

**Что сделано**:
1. ✅ Создан `core/domain/stuck_rules.py`:
   - get_blocker_description(blocker_type) — описания для AI промптов
   - get_blocker_emoji(blocker_type) — эмодзи для UI
   - is_valid_blocker(blocker_type) — валидация типа блокера
   - normalize_blocker_type(blocker_type) — нормализация в enum
   - should_request_details(blocker_type) — нужны ли детали
   - calculate_microhit_count(blocker_type, has_details) — сколько вариантов генерировать (2-3)

2. ✅ Создан `core/use_cases/resolve_stuck.py`:
   - ResolveStuckUseCase с методами:
     - get_stuck_context(user, goal) — получение контекста (step/stage)
     - generate_microhit_options(step_title, blocker_type, details, count) → MicrohitOptionsResult
   - Result dataclasses: MicrohitOption, MicrohitOptionsResult, StuckContextResult

3. ✅ Обновлен `src/bot/callbacks/data.py`:
   - Добавлен MicrohitOptionCallback для выбора варианта

4. ✅ Обновлен `src/bot/keyboards.py`:
   - Добавлена функция microhit_options_keyboard(options, blocker, step_id)
   - Показывает варианты с эмодзи (1️⃣, 2️⃣, 3️⃣)

5. ✅ Рефакторирован `src/bot/handlers/stuck.py`:
   - Использует ResolveStuckUseCase вместо прямого вызова ai_service
   - Handler стал тонким — только UI логика
   - Новая функция: generate_and_show_microhit_options() вместо generate_and_show_microhit()
   - Новый handler: microhit_option_selected() для выбора варианта
   - Обновлены handlers для blocker_other, process_details, microhit_feedback
   - Кнопка "Ещё варианты" теперь генерирует НОВЫЙ набор вариантов

6. ✅ Проверены импорты и синтаксис: успешно
7. ✅ Проверены use-case методы и domain функции: всё работает

### Результат:

**Было** (stuck.py):
- Генерация одного микро-удара
- Кнопка "Ещё вариант" → новый одиночный микро-удар
- Вся логика в handler (AI вызовы, blocker описания)

**Стало**:
- **Handler** (stuck.py): тонкий, только UI-логика (callbacks, keyboards, FSM)
- **Репозитории**: не добавлялись (используем существующие)
- **Домейн** (stuck_rules.py): чистые функции для blocker logic
- **Use-case** (resolve_stuck.py): оркестрация AI + context + генерация вариантов

**Ключевое улучшение Stage 2.3:**
Вместо показа ОДНОГО микро-удара и ожидания запроса "ещё",
теперь генерируется 2-3 варианта СРАЗУ для выбора пользователем.

**Flow:**
1. /stuck → выбор блокера → генерация 2-3 вариантов
2. Пользователь выбирает подходящий → показывается с кнопками "Делаю" / "Ещё варианты" / "Другое"
3. Кнопка "Ещё варианты" → генерируется НОВЫЙ набор из 2-3 вариантов

**Следующий шаг**: Этап 2.4 - Вечерний сценарий (Evening Reflection Use-Case)

---

#### ✅ 2.4: Вечерний сценарий (Evening Reflection Use-Case)

**Статус**: Завершен
**Дата**: 2025-12-13
**Коммит**: (будет создан) - refactor(stage-2.4): extract evening reflection to use-case

**Что сделано**:
1. ✅ Создан `core/domain/reflection_rules.py`:
   - calculate_daily_progress(daily_log, steps) — подсчет статистики дня
   - format_steps_summary(steps) — форматирование списка шагов с эмодзи
   - should_show_streak_celebration(streak_days) — определение особого празднования streak
   - format_streak_text(streak_days) — форматирование текста streak

2. ✅ Создан `core/use_cases/complete_daily_reflection.py`:
   - CompleteDailyReflectionUseCase с методами:
     - get_daily_summary(user, today) → DailySummaryResult
     - complete_day(user, today) → DayCompletionResult
   - Result dataclasses: DailySummaryResult, DayCompletionResult

3. ✅ Рефакторирован `src/bot/handlers/evening.py`:
   - Использует CompleteDailyReflectionUseCase вместо локальных функций
   - Handler стал тонким — только UI логика
   - Удалена функция update_streak (теперь в gamification.calculate_streak)
   - Упрощена функция finish_day (теперь только отображение результата)

4. ✅ Проверены импорты и синтаксис: успешно
5. ✅ Ruff check: успешно

### Результат:

**Было** (evening.py):
- Вся бизнес-логика в handler (streak, stats, форматирование)
- Функция update_streak в handler (9 строк)
- Функция finish_day в handler (56 строк с логикой)

**Стало**:
- **Handler** (evening.py): тонкий, только UI-логика (клавиатуры, сообщения, FSM)
- **Домейн** (reflection_rules.py): чистые функции для расчета статистики и форматирования
- **Use-case** (complete_daily_reflection.py): оркестрация DailyLog + steps + gamification
- **Gamification** (используем существующий calculate_streak из gamification.py)

**Ключевые улучшения Stage 2.4:**
- Вся логика streak вынесена в domain layer (gamification.py)
- Все расчеты статистики в domain layer (reflection_rules.py)
- Handler теперь только вызывает use-case и показывает результат
- Готово для использования из TMA API

**Следующий шаг**: Этап 2.5 - Закрыть баг с "Изменить"

---

#### ✅ 2.5: Закрыть баг с "Изменить"

**Статус**: Завершен
**Дата**: 2025-12-13
**Коммит**: (будет создан) - fix(stage-2.5): remove unused confirm callbacks and keyboards

**Что сделано**:
1. ✅ Исследован баг:
   - Кнопка "Изменить" и handler'ы были удалены в Stage 1.2 при упрощении onboarding
   - Но клавиатуры (confirm_keyboard, confirm_with_cancel_keyboard, yes_no_keyboard) остались
   - ConfirmCallback и ConfirmAction остались в callbacks/data.py, но нигде не использовались

2. ✅ Удалены неиспользуемые элементы из keyboards.py:
   - confirm_keyboard() (9 строк)
   - confirm_with_cancel_keyboard() (11 строк)
   - yes_no_keyboard() (9 строк)
   - Удалены импорты ConfirmCallback и ConfirmAction

3. ✅ Удалены неиспользуемые элементы из callbacks/data.py:
   - ConfirmAction enum (7 строк)
   - ConfirmCallback class (12 строк)

4. ✅ Проверены импорты и синтаксис: успешно
5. ✅ Ruff check: успешно

### Результат:

**Было**:
- Неиспользуемые клавиатуры с кнопкой "Изменить" (29 строк кода)
- Неиспользуемые ConfirmCallback и ConfirmAction (19 строк кода)
- Потенциальная путаница: код определен, но handler'ы отсутствуют

**Стало**:
- Удален весь неиспользуемый код (~48 строк)
- Чистый кодбаза без мертвого кода
- Нет риска ошибок от необработанных callback'ов

**Заключение**:
Баг был не в том, что кнопка "Изменить" удаляет цель, а в том, что после упрощения onboarding в Stage 1.2 остались неиспользуемые клавиатуры и callback'ы. Это могло привести к ошибкам, если бы эти клавиатуры случайно использовались без handler'ов.

**Следующий шаг**: Этап 2.6 - Финальная проверка

---

#### ⬜ 2.6: Финальная проверка

**Статус**: Не начато

**Что делать**:
1. Прогнать все тесты: `pytest`
2. Прогнать руками основные флоу:
   - Новый пользователь → цель → /morning → выполнить шаг → /evening
   - /stuck → выбрать блокер → микроудар → выполнить
3. Обновить `docs/TMA_PROGRESS.md` — отметить Этап 2 выполненным
4. Коммит: `docs(stage-2.6): update TMA_PROGRESS.md for Stage 2 completion`

**Результат**: Этап 2 завершён, логика готова для Этапа 3/4

---

### Критические правила для Этапа 2:

1. ✅ **НЕ "переписать всё"** — по одному сценарию, прогнать руками, закоммитить
2. ✅ **Репозитории = тупые** — только CRUD, без бизнес-логики
3. ✅ **Домейн = чистые функции** — без БД, без Telegram
4. ✅ **Use-cases = оркестрация** — связывают репозитории + домейн + AI
5. ✅ **Handlers = тонкие** — принять ввод → вызвать use-case → показать результат
6. ✅ **Тест после каждого sub-stage** — руками + pytest
7. ✅ **Цель НЕ "идеальная архитектура"** — а чтобы Этап 3/4 стали механическими

---

## 📊 Метрики прогресса

**Размер кодовой базы handlers/**:

| До              | После Этапа 1.1 | Цель после Этапа 1 |
|-----------------|------------------|---------------------|
| ~70KB           | ~54.5KB          | ~40KB              |

**Модели БД**:

| До              | После Этапа 1.1 | Цель после Этапа 1 |
|-----------------|------------------|---------------------|
| User, Goal, Stage, Step, DailyLog, QuizResult | User, Goal, Stage, Step, DailyLog | То же (QuizResult удален) |

---

## 🐛 Известные баги (чинить после Этапа 1)

1. **Кнопка "Изменить" удаляет цель** (критично)
   - Должна редактировать этапы, а не удалять цель
   - Нужна команда `/edit_goal` для изменения названия
   - Нужна кнопка "Удалить цель" с подтверждением

---

## 🚀 Следующие улучшения (после TMA MVP)

См. `docs/BACKLOG.md`:
- Замена OpenAI → Claude Sonnet 4.5
- Жесткий тон (пуш к действию)
- Альтернативные варианты микродействий
- Реалистичное планирование с калибровкой
- Усиленная геймификация

---

**Последнее обновление**: 2025-12-12, **Этап 1 ПОЛНОСТЬЮ ЗАВЕРШЕН** (1.1-1.5) ✅
