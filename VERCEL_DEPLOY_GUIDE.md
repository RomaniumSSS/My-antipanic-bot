# Деплой TMA на Vercel через Dashboard

**Проблема**: npm требует права sudo для локального деплоя.
**Решение**: Используем Vercel Dashboard (веб-интерфейс) — проще и быстрее.

---

## Шаг 1: Подготовка репозитория

### 1.1. Закоммить изменения Stage 4.3

```bash
cd /Users/ramanpazharytski/My-antipanic-bot

git add .
git commit -m "feat(stage-4.3): integrate TMA with bot via WebApp button

- Add TMA_URL to config
- Add WebApp button to main menu keyboard
- Add /app command to launch TMA
- Update /help to include /app
- Add comprehensive TMA setup documentation"

git push origin feature/tma-migration
```

### 1.2. Убедиться что код на GitHub

```bash
git remote -v
# Проверить что origin указывает на твой репозиторий
```

---

## Шаг 2: Деплой на Vercel

### 2.1. Открыть Vercel Dashboard

1. Зайди на https://vercel.com
2. Войди через GitHub аккаунт (или создай аккаунт)

### 2.2. Создать новый проект

1. Нажми **"Add New..."** → **"Project"**
2. **Import Git Repository**:
   - Выбери свой репозиторий `My-antipanic-bot`
   - Если не видишь → нажми "Adjust GitHub App Permissions" → разреши доступ

### 2.3. Настроить проект

**Important Settings:**

- **Project Name**: `antipanic-tma` (или любое другое)
- **Framework Preset**: `Next.js` (должно автоматически определиться)
- **Root Directory**: `tma-frontend` ⚠️ **ВАЖНО!**
  - Нажми "Edit" → введи `tma-frontend`
  - Это папка где находится Next.js приложение

**Build and Output Settings:**
- Build Command: `npm run build` (оставить по умолчанию)
- Output Directory: `.next` (оставить по умолчанию)
- Install Command: `npm install` (оставить по умолчанию)

### 2.4. Environment Variables

Добавь переменную окружения:

- **Name**: `NEXT_PUBLIC_API_URL`
- **Value**: `https://your-bot.railway.app` 
  - ⚠️ Замени на **реальный URL твоего бота на Railway**
  - Например: `https://antipanic-bot-production.up.railway.app`

Как найти Railway URL:
1. Зайди в Railway Dashboard
2. Откройти твой проект → сервис бота
3. Вкладка **Settings** → **Domains**
4. Скопируй публичный URL

### 2.5. Deploy

Нажми **"Deploy"**

Vercel:
- Установит зависимости
- Соберёт Next.js приложение
- Задеплоит на свой CDN

⏱️ Процесс займёт ~2-5 минут

---

## Шаг 3: Получить URL

После успешного деплоя:

1. Vercel покажет **"Congratulations!"**
2. Скопируй URL деплоя:
   ```
   https://antipanic-tma.vercel.app
   ```
   (или другой домен который Vercel назначил)

3. **СОХРАНИ ЭТОТ URL** — он понадобится для следующих шагов

---

## Шаг 4: Настроить @BotFather

### 4.1. Создать Web App

1. Открой [@BotFather](https://t.me/BotFather) в Telegram
2. Отправь `/mybots`
3. Выбери своего бота
4. **Web App** → **Create Web App**
5. Введи данные:
   - **Title**: `Antipanic App`
   - **Description**: `Микро-действия для преодоления прокрастинации`
   - **Photo**: (опционально)
   - **Web App URL**: `https://antipanic-tma.vercel.app` ⚠️ **URL из Vercel**
   - **Short name**: `antipanic`

6. Подтверди

### 4.2. (Опционально) Menu Button

Добавь кнопку Menu для быстрого доступа:

1. В @BotFather → `/mybots` → твой бот
2. **Bot Settings** → **Menu Button** → **Edit Menu Button URL**
3. Введи: `https://antipanic-tma.vercel.app`

---

## Шаг 5: Добавить TMA_URL в Railway

### 5.1. Открыть Railway Dashboard

1. Зайди на https://railway.app
2. Открой свой проект
3. Выбери сервис **бота** (не PostgreSQL/Redis)

### 5.2. Добавить переменную

1. Вкладка **Variables**
2. Нажми **+ New Variable**
3. Добавь:
   ```
   TMA_URL=https://antipanic-tma.vercel.app
   ```
   ⚠️ Используй реальный URL из Vercel

4. Нажми **Add**

### 5.3. Redeploy

Railway автоматически перезапустит бот с новой переменной.

Подожди ~1-2 минуты пока бот перезапустится.

---

## Шаг 6: Проверить работу

### 6.1. Проверить бот

1. Открой своего бота в Telegram
2. Напиши `/start`
3. **Должна появиться кнопка "📱 App"** в меню
4. Если нет — подожди 1 минуту (бот перезапускается)

### 6.2. Открыть TMA

1. Нажми на кнопку **"📱 App"**
2. Telegram откроет Mini App
3. Должен появиться интерфейс с:
   - Приветствием
   - Профилем пользователя
   - Полем для ввода задачи

### 6.3. Проверить функционал

**Если видишь "This app only works inside Telegram":**
- ✅ Это нормально! Значит TMA работает
- Открывай TMA только через Telegram бота

**Проверка API:**
1. В TMA должен отобразиться твой профиль (имя, username)
2. Если показывается "Loading..." долго → проблема с API
3. Проверь что `NEXT_PUBLIC_API_URL` правильный

**Генерация микро-действий:**
1. Введи задачу (например, "Написать код")
2. Выбери blocker (например, "fear")
3. Нажми "Generate"
4. Должны появиться 2-3 варианта микро-действий

### 6.4. Проверить команду /app

1. Напиши `/app` в боте
2. Должно появиться сообщение с кнопкой "🚀 Открыть App"
3. Нажми → TMA откроется

---

## Troubleshooting

### Проблема: Кнопка "📱 App" не появляется

**Причина**: `TMA_URL` не задан или бот не перезапустился

**Решение**:
1. Проверь Railway Variables → `TMA_URL` должен быть там
2. Railway → Deployments → нажми "Redeploy"
3. Подожди 1-2 минуты
4. Напиши `/start` снова

---

### Проблема: TMA показывает "Loading..." вечно

**Причина**: TMA не может подключиться к FastAPI backend

**Решение**:
1. Vercel Dashboard → твой проект → Settings → Environment Variables
2. Проверь `NEXT_PUBLIC_API_URL`:
   - Должен быть `https://твой-бот.railway.app`
   - БЕЗ `/api` в конце
3. Если неправильный → измени → Redeploy на Vercel

**Проверить Railway URL:**
```bash
# В Railway Dashboard
Your Project → Bot Service → Settings → Domains
```

---

### Проблема: TMA показывает ошибку "Unauthorized"

**Причина**: Проблема с Telegram auth

**Решение**:
1. Убедись что TMA открыт через Telegram (не в браузере)
2. Проверь Railway логи бота:
   - Railway → твой проект → Bot → Logs
   - Ищи ошибки связанные с "auth" или "initData"

---

### Проблема: Vercel не видит репозиторий

**Причина**: Нет доступа к GitHub репозиторию

**Решение**:
1. Vercel Dashboard → Add New → Import Git Repository
2. Нажми "Adjust GitHub App Permissions"
3. Разреши доступ к репозиторию `My-antipanic-bot`

---

### Проблема: Build fails на Vercel

**Причина**: Ошибка в коде или зависимостях

**Решение**:
1. Vercel Dashboard → Deployments → Failed deployment → View Logs
2. Прочитай логи — там будет причина
3. Если проблема с зависимостями:
   ```bash
   cd tma-frontend
   rm -rf node_modules package-lock.json
   npm install
   ```
4. Закоммить `package-lock.json` и пушнуть

---

## Альтернатива: CLI деплой (если права есть)

Если получится установить Vercel CLI:

```bash
# Установить с sudo (macOS/Linux)
sudo npm install -g vercel

# Или без sudo (локально для проекта)
cd tma-frontend
npm install vercel

# Деплой
npx vercel

# Следовать инструкциям:
# - Link to existing project? → No
# - Project name? → antipanic-tma
# - Directory? → ./ (оставить по умолчанию)
# - Override settings? → No

# Production deploy
npx vercel --prod
```

---

## Итог

После выполнения всех шагов:

✅ TMA задеплоен на Vercel
✅ @BotFather настроен (Web App URL)
✅ Railway настроен (TMA_URL)
✅ Кнопка "📱 App" работает в боте
✅ TMA открывается и показывает UI
✅ API работает (профиль, микро-действия)

**Stage 4.3 полностью завершён!** 🎉

---

## Полезные ссылки

- **Vercel Dashboard**: https://vercel.com/dashboard
- **Railway Dashboard**: https://railway.app/dashboard
- **@BotFather**: https://t.me/BotFather
- **Vercel Docs**: https://vercel.com/docs
- **Next.js Docs**: https://nextjs.org/docs

---

**Документация**: См. `docs/TMA_SETUP.md` для дополнительных деталей.
