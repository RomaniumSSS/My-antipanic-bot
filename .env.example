# Telegram Bot Configuration
BOT_TOKEN=your_telegram_bot_token_here

# OpenAI Configuration
OPENAI_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini

# Environment (development | production)
ENVIRONMENT=development

# Alpha Testing: Whitelist (comma-separated user IDs, empty = open access)
ALLOWED_USER_IDS=

# === Production Settings (Railway/Render) ===

# Database URL (Railway format) - auto-provided by Railway PostgreSQL
# Supports both postgres:// and postgresql:// schemes (auto-converted)
# If not set, will use individual POSTGRES_* vars below
# Example: postgres://user:pass@host:5432/dbname
DATABASE_URL=

# PostgreSQL (alternative to DATABASE_URL)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=antipanic
POSTGRES_USER=antipanic
POSTGRES_PASSWORD=

# Redis (for FSM storage in production)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Webhook (for production deployment)
# Railway sets PUBLIC_URL automatically, or you can override
WEBHOOK_URL=https://your-app.railway.app
WEBHOOK_PATH=/webhook

# Port (auto-set by Railway, default 8080)
PORT=8080

# Cron token (for /cron/tick endpoint security)
# Generate a random string: openssl rand -hex 32
CRON_TOKEN=your_random_secret_token_here
